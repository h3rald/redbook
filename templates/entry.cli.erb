<%
padding = ' '*Math.log10(@total).round
%><%= "#{padding}#{@index.to_s.cyan} #{@entry.timestamp.textualize.dark_cyan}" %> <%= @entry.text.cyan -%><%= 
@partial.call :_tags, :tags => @entry.tags if @entry.respond_to? :tags %>

