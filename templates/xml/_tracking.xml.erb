		<start><%= @object.activity.start -%></start>
		<end><%= @object.activity.end -%></end>
		<foreground><%= @object.activity.foreground -%></foreground>
		<tracking><%= @object.activity.tracking -%></tracking>
		<duration tracked="<%= @object.activity.tracked_duration -%>"><%= @object.activity.duration -%></duration><% if @object.chain(:records, :length) > 0  then%>
		<records><%  @object.records.each do |r| %>
			<record start="<%= r.start %>" end="<%= r.end %>" /><% end %>
		</records><% end %>
