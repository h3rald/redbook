	<activity>
		<text><%= @object.text -%></text>
		<timestamp><%= @object.timestamp -%></timestamp><%= 
			@partial.call :tags if @object.respond_to? :tags -%><%=
			@partial.call :detail if @detail %><%=
			@partial.call :tracking if @tracking %>
	</activity>

